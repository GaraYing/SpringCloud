server:
  port: 9999
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GaraYing/config-repo
#          force-pull: true
          skip-ssl-validation: true
#          force-pull: true
          search-paths: /config-repo
#          skip-ssl-validation: true
management:
  endpoints:
    web:
      exposure:
        include: '*'